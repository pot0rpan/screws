import Layout from '../../components/layout/Layout';
import CleanUrl from '../../components/CleanUrl';

const UnscrewPage = () => {
  return (
    <Layout title="/ Clean a URL">
      <CleanUrl />
    </Layout>
  );
};

export default UnscrewPage;
